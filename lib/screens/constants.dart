class Constants {
  static const String desc =
      "To use Hero animation within a Stack widget, you need to ensure that both the source and destination widgets are wrapped inside a Hero widget with the same tag. The tag serves as an identifier to match the two widgets and animate the transition between them.";
}
